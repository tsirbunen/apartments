// [id, level, name] — the triple Oikotie uses in its locations param
export type OikotieLocation = [number, number, string]

export const DEFAULT_LOCATIONS: OikotieLocation[] = [
  [1669, 4, 'Lauttasaari, Helsinki'],
]
